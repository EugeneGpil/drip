const state = () => ({})
const actions = {}

import {defineStore} from 'pinia'
import getters from './getters'

export const useIntervalAndSavingsStore = defineStore('intervalAndSavingsStore', {
  state,
  actions,
  getters,
})
